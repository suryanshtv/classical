/*
 * mapping of songs per queue as an array for canvas integration
 */

const { registerFont, createCanvas, loadImage } = require("canvas");
const pms = require("pretty-ms");

module.exports = {
    async mapForQueue(queuedSongMap, queueLength, page) {


    }

}